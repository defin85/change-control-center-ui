# Legacy Prototype

Это reference-only зона для оставшихся артефактов статического прототипа.

- Активный application path находится в `backend/`, `web/` и `scripts/ccc`.
- Полный ранний prototype path частично остался только в git history; в текущем working tree здесь храните только reference artifacts, которые не должны мешать онбордингу в основную архитектуру.
